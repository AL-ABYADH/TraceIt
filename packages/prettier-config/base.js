/**
 * A shared Prettier configuration for the repository.
 *
 * @type {import("prettier").Config}
 * */
export const config = {
  semi: true,
  singleQuote: false,
  trailingComma: "all",
  printWidth: 100,
  tabWidth: 2,
};
